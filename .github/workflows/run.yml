name: تشغيل البوت

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 سحب الكود
      uses: actions/checkout@v3
    
    - name: 🐍 إعداد Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 📦 تثبيت المكتبات
      run: pip install -r requirements.txt
    
    - name: 🤖 تشغيل البوت
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
      run: |
        echo "========================================"
        echo "🚀 بدء تشغيل بوت حسين"
        echo "🕒 الوقت: $(date)"
        echo "========================================"
        python bot.py
